if needActionsContents['waiting_sound'].length
    table.table.table-hover.table-bordered#contentsWaitingSoundDataTable
        thead
            tr
                th Titre          
                th Famille            
                th Date de mise à jour
        tbody
            each content in needActionsContents['waiting_sound']
                tr
                    td
                        a(href="/contents/edit/" + content.id data-id=`${content.id}`)
                            | #{content.title}
                    td
                        if content.itsTheme
                            | #{content.itsTheme}
                            
                    td(data-sort=content.updatedAt) #{prettyDate(content.updatedAt)}
else
    Il n'y a pas de contenu en attente d'audio pour le moment.